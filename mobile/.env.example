# IP locale du PC qui fait tourner le backend (pas "localhost" : le téléphone est un
# appareil différent). Trouver l'IP avec `ipconfig` (Windows) → carte Wi-Fi.
# Modifiable aussi depuis l'app (écran Réglages) sans reconstruire.
EXPO_PUBLIC_API_URL=http://192.168.1.100:3001
